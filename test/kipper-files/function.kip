const int_list: list<int> = [1, 2, 3, 4];

// Get sum of all items stored inside 'param'
def get_sum(param: list<int>) -> int {
  sum: int = 0;
  for (int i = 0; i < param.len; i++) {
	sum += param[i];
  }
  return sum;
}

call print(f"The sum of the following list: {int_list}");

// Calculating the sum of the items in the list
var result = call getSumOf(int_list);
call print(f"Result: {result}");

// Exiting the program
call exit(0);
